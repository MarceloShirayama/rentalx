{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "rentalx",
  "password": "rentalx",
  "database": "rentalx",
  "migrations": ["./src/database/migrations/*.ts"],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
